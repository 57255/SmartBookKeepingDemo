package com.example.smartbookkeeping.logic

import androidx.lifecycle.liveData
import com.example.smartbookkeeping.logic.model.SubmitBill
import kotlinx.coroutines.Dispatchers

object Repository {
    fun submitBill(submitBills: SubmitBill)=liveData(Dispatchers.IO){
        val result=try{
            val
        }
    }
}